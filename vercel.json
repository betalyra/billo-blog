{
    "installCommand": "pnpm -r install",
    "buildCommand": "pnpm -r build",
    "outputDirectory": "apps/server",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/apps/server/api/serverless.js"
        }
    ]
}
