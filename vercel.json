{
    "installCommand": "pnpm -r install",
    "buildCommand": "pnpm -r build",
    "outputDirectory": "app/server",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/app/server/api/serverless.js"
        }
    ]
}
